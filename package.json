{
  "name": "ai-coding-assistant-v2",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "echo 'Development server not needed for static HTML'",
    "build": "echo 'No build step needed' && mkdir -p dist && cp -r * dist/",
    "preview": "echo 'Preview not available'"
  }
}